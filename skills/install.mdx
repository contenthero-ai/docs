---
title: "Install the Skill"
description: "Install the ContentHero skill, or the plugin that brings the connector with it, on the AI you already use."
---

{/* GENERATED by scripts/generate-skill-install.mjs from @contenthero-ai/connect. Do not edit. */}

ContentHero ships as one plugin: the `contenthero` skill plus the ContentHero connector. Where your AI
installs plugins, one install brings both. Where it only takes a skill, install the skill and
[connect ContentHero](/mcp/overview) separately, because a skill on its own cannot bring a connector.

Every release publishes two files:

- [`contenthero-plugin.zip`](https://github.com/contenthero-ai/skills/releases/latest/download/contenthero-plugin.zip): the plugin, skill and connector together.
- [`contenthero.zip`](https://github.com/contenthero-ai/skills/releases/latest/download/contenthero.zip): the skill on its own.

<Tabs>
  <Tab title="Claude">
    <Steps>
      <Step title="Download the plugin">
        One file with the ContentHero skill and connector together. Plugins need a paid Claude plan.

        [Download the plugin](https://github.com/contenthero-ai/skills/releases/latest/download/contenthero-plugin.zip)
      </Step>
      <Step title="Upload it">
        In Claude, go to Customize, Plugins, click Add, and upload the file.

        [Open Claude](https://claude.ai/customize/plugins)
      </Step>
      <Step title="Connect and start">
        On the plugin's Connectors tab, click Connect next to ContentHero if it isn't connected yet. Then type /contenthero or just ask.
      </Step>
    </Steps>

    Tested by a real install on 2026-09-24.
  </Tab>
  <Tab title="Claude Code">
    <Steps>
      <Step title="Add the marketplace">
        Run this in your terminal. The plugin brings the skill and the ContentHero connector together.

        ```bash
        claude plugin marketplace add contenthero-ai/skills
        ```
      </Step>
      <Step title="Install the plugin">
        Install ContentHero from that marketplace.

        ```bash
        claude plugin install contenthero@contenthero
        ```
      </Step>
      <Step title="Sign in">
        Run /mcp in a session and sign in to ContentHero. Then type /contenthero or just ask.
      </Step>
    </Steps>

    Tested by a real install on 2026-09-24.
  </Tab>
  <Tab title="ChatGPT">
    <Steps>
      <Step title="Download the skill">
        The ContentHero skill comes as one file.

        [Download the skill](https://github.com/contenthero-ai/skills/releases/latest/download/contenthero.zip)
      </Step>
      <Step title="Upload it">
        In ChatGPT, open Plugins, switch to the Skills tab, click the plus, and upload the file.

        [Open ChatGPT](https://chatgpt.com/skills)
      </Step>
      <Step title="Connect ContentHero">
        The skill works through your ContentHero connection. If you haven't connected yet, follow the MCP steps for ChatGPT.
      </Step>
    </Steps>

    Tested by a real install on 2026-09-24.
  </Tab>
  <Tab title="Codex">
    <Steps>
      <Step title="Add the marketplace">
        Run this in your terminal. The plugin brings the skill and the ContentHero connector together.

        ```bash
        codex plugin marketplace add contenthero-ai/skills
        ```
      </Step>
      <Step title="Install the plugin">
        Start codex, open /plugins, install ContentHero, then start a new session.

        ```bash
        /plugins
        ```
      </Step>
      <Step title="Sign in">
        Approve the ContentHero sign-in when Codex asks. Then just ask.
      </Step>
    </Steps>

    Checked against the host's own documentation on 2026-09-24.
  </Tab>
  <Tab title="Gemini">
    <Steps>
      <Step title="Download and unzip">
        Download the skill, then double-click it to unzip. Gemini takes the folder, not the zip.

        [Download the skill](https://github.com/contenthero-ai/skills/releases/latest/download/contenthero.zip)
      </Step>
      <Step title="Upload the folder">
        In Gemini, switch to Spark, open Skills, click upload, and choose the contenthero folder. Needs AI Pro or Ultra.

        [Open Gemini](https://gemini.google.com)
      </Step>
      <Step title="Connect ContentHero">
        The skill works through your ContentHero connection. If you haven't connected yet, follow the MCP steps for Gemini.
      </Step>
    </Steps>

    Tested by a real install on 2026-09-24.
  </Tab>
  <Tab title="Gemini CLI">
    <Steps>
      <Step title="Install the skill">
        Run this in your terminal. Gemini CLI needs an enterprise license or a paid API key.

        ```bash
        gemini skills install https://github.com/contenthero-ai/skills --path skills/contenthero --scope user
        ```
      </Step>
      <Step title="Connect ContentHero">
        The skill works through your ContentHero connection. If you haven't connected yet, follow the MCP steps for Gemini CLI.
      </Step>
      <Step title="Just ask">
        Describe the work in plain language. The skill loads itself when it recognizes the job.
      </Step>
    </Steps>

    Checked against the host's own documentation on 2026-09-24.
  </Tab>
  <Tab title="Antigravity">
    <Steps>
      <Step title="Download and unzip">
        Download the skill, then double-click it to unzip.

        [Download the skill](https://github.com/contenthero-ai/skills/releases/latest/download/contenthero.zip)
      </Step>
      <Step title="Add the folder">
        Move the contenthero folder into your skills folder. For the Antigravity CLI, use ~/.gemini/antigravity-cli/skills instead.

        ```bash
        ~/.gemini/config/skills
        ```
      </Step>
      <Step title="Connect ContentHero">
        The skill works through your ContentHero connection. If you haven't connected yet, follow the MCP steps for Antigravity.
      </Step>
    </Steps>

    Checked against the host's own documentation on 2026-09-24.
  </Tab>
  <Tab title="Grok">
    <Steps>
      <Step title="Download the skill">
        The ContentHero skill comes as one file.

        [Download the skill](https://github.com/contenthero-ai/skills/releases/latest/download/contenthero.zip)
      </Step>
      <Step title="Upload it">
        Go to grok.com/skills and upload the file.

        [grok.com/skills](https://grok.com/skills)
      </Step>
      <Step title="Connect ContentHero">
        The skill works through your ContentHero connection. If you haven't connected yet, follow the MCP steps for Grok.
      </Step>
    </Steps>

    Checked against the host's own documentation on 2026-09-24.
  </Tab>
  <Tab title="Cursor">
    <Steps>
      <Step title="Import the plugin">
        In Cursor, open Customize, choose From GitHub Repository, and paste this URL.

        ```bash
        https://github.com/contenthero-ai/skills
        ```
      </Step>
      <Step title="Install it">
        Install ContentHero and choose User scope, so it works in every project. It brings the connector too.
      </Step>
      <Step title="Sign in">
        Authorize ContentHero in Settings, MCP. Then just ask.
      </Step>
    </Steps>

    Checked against the host's own documentation on 2026-09-24.
  </Tab>
  <Tab title="VS Code">
    <Steps>
      <Step title="Turn on plugins">
        Plugins are still experimental in VS Code. In Settings, turn this on.

        ```bash
        chat.plugins.enabled
        ```
      </Step>
      <Step title="Install the plugin">
        Run Chat: Install Plugin From Source from the Command Palette and paste this URL. It brings the connector too.

        ```bash
        https://github.com/contenthero-ai/skills
        ```
      </Step>
      <Step title="Sign in">
        Approve the ContentHero sign-in, then ask in agent mode.
      </Step>
    </Steps>

    Checked against the host's own documentation on 2026-09-24.
  </Tab>
  <Tab title="OpenClaw">
    <Steps>
      <Step title="Download and unzip">
        Download the skill, then double-click it to unzip.

        [Download the skill](https://github.com/contenthero-ai/skills/releases/latest/download/contenthero.zip)
      </Step>
      <Step title="Install the skill">
        Run this from the folder you unzipped it in.

        ```bash
        openclaw skills install ./contenthero
        ```
      </Step>
      <Step title="Connect ContentHero">
        The skill works through your ContentHero connection. If you haven't connected yet, follow the MCP steps for OpenClaw.
      </Step>
    </Steps>

    Checked against the host's own documentation on 2026-09-24.
  </Tab>
  <Tab title="Hermes">
    <Steps>
      <Step title="Install the skill">
        Run this in your terminal. Hermes scans every skill before installing it.

        ```bash
        hermes skills install contenthero-ai/skills/skills/contenthero
        ```
      </Step>
      <Step title="Connect ContentHero">
        The skill works through your ContentHero connection. If you haven't connected yet, follow the MCP steps for Hermes.
      </Step>
      <Step title="Just ask">
        Describe the work in plain language. The skill loads itself when it recognizes the job.
      </Step>
    </Steps>

    Checked against the host's own documentation on 2026-09-24.
  </Tab>
</Tabs>

## Updating

A plugin installed from the marketplace updates with the marketplace. An uploaded file is a
snapshot: download the new release and upload it again.

<Warning>
Never paste your API key into an agent chat. Use the connector, `contenthero login`, or the `CONTENTHERO_API_KEY` environment variable.
</Warning>
