---
title: "Brand"
description: "Brand kits and the knowledge base that grounds generations in your voice."
---

{/* GENERATED FILE. Do not edit by hand. */}
{/* Regenerate with `npm run generate:reference` in contenthero-docs. */}

Brand kits and the knowledge base that grounds generations in your voice. 9 tools.

Every tool below is exposed by the hosted MCP server at `https://mcp.contenthero.ai` and by the `@contenthero/mcp` npm package. A <Badge>write</Badge> tool changes your account; a <Badge>read</Badge> tool does not.

### `list_brand_kits`

<Badge>read</Badge>

List the account's brand kits (default first). Excludes archived kits unless archived=true; set favorited=true for only favorites. Call get_brand_kit for one kit's full brand context (voice, visual identity, audience, sections, accounts, knowledge) to write on-brand content.

<ParamField body name="archived" type="boolean">
Only archived brand kits (default excludes archived).
</ParamField>

<ParamField body name="favorited" type="boolean">
Only favorited brand kits.
</ParamField>

### `get_brand_kit`

<Badge>read</Badge>

Get one brand kit in full: business overview, positioning, audience, voice profile, visual identity (logos/colors/typography), curated sections, linked brand + inspiration accounts, and a knowledge-base summary. Use it to ground on-brand generation.

<ParamField body name="brandKitId" type="string" required>
The brand kit id from list_brand_kits.
</ParamField>

### `create_brand_kit`

<Badge>write</Badge>

Create a brand kit. THREE SOURCES, chosen by what you pass: (1) EMPTY, just a name, then fill it in with update_brand_kit; or FROM A SOCIAL PROFILE, pass its url in brandAccounts (your own) or inspirationAccounts (a creator you watch) with no name at all, and the kit is named after the handle and starts ingesting that account's posts if it is YouTube or Instagram; (2) FROM A WEBSITE, pass websiteUrl + extract:true and ContentHero scrapes that site and fills in business name, positioning, voice, colors, typography, logos and assets by itself, which is by far the fastest way to get a real kit; (3) A COPY, pass duplicateFrom with an existing kit id, which copies its sections and brand media (assets re-link rather than duplicate, so a copy costs no storage). A brand with NO WEBSITE (so nothing to extract) is built by passing its fields directly, including logos, whose entries may name outputId to bring in a generation you just made rather than a url. With extract it RETURNS IMMEDIATELY, before the kit has any content: that empty kit is the handle, and the fields fill in over the next minute or two, so poll extractionStatus with get_brand_kit rather than assuming it failed. name is OPTIONAL when websiteUrl or a social profile url is given: it defaults to the site's hostname or the @handle, a placeholder extraction or you overwrite later. Brand kits are capped by plan, so this fails with a limit error near the cap, and a duplicate counts against it like any other kit. Requires the brandkit:write scope.

<ParamField body name="assets" type="object[]">
The kit's brand assets, each \{ url | outputId, name? \}.
</ParamField>

<ParamField body name="audience" type="object">
Audience object (free-form).
</ParamField>

<ParamField body name="brandAccounts" type="value[]">
The account owner's OWN profiles. A tracked-account id, or \{ platform?, handleOrUrl \} to ADD one and start ingesting it.
</ParamField>

<ParamField body name="businessName" type="string">
The business or creator name this kit represents.
</ParamField>

<ParamField body name="contentStrategy" type="object">
Content strategy object (free-form).
</ParamField>

<ParamField body name="designPrinciples" type="string[]">
Design rules to hold to, one per entry. REPLACES the list; [] clears it.
</ParamField>

<ParamField body name="duplicateFrom" type="string">
Copy an existing brand kit id instead of starting empty.
</ParamField>

<ParamField body name="extract" type="boolean">
Scrape websiteUrl and fill the kit in automatically. Returns at once; poll extractionStatus.
</ParamField>

<ParamField body name="inspirationAccounts" type="value[]">
Competitor/creator profiles they watch. Same entry shape as brandAccounts.
</ParamField>

<ParamField body name="logos" type="object[]">
The kit's logos, each \{ url | outputId, name?, is_primary?, layout?, colorMode? \}. Use outputId to bring in a generation.
</ParamField>

<ParamField body name="name" type="string">
The kit's name. Optional when websiteUrl is given.
</ParamField>

<ParamField body name="nicheDefinition" type="string">
The niche this brand operates in. Keeps generated angles on-topic.
</ParamField>

<ParamField body name="positioning" type="object">
Positioning object (free-form).
</ParamField>

<ParamField body name="primaryOffer" type="string">
What this business sells, in one line. Grounds copy in what is actually being promoted.
</ParamField>

<ParamField body name="sections" type="object[]">
The kit's curated sections, each \{ tab, sectionName, sortOrder?, fields? \}. Array position is the default order.
</ParamField>

<ParamField body name="visualStyle" type="string">
The look in words, e.g. 'warm film grain, muted earth tones'. Grounds image and video prompts.
</ParamField>

<ParamField body name="voiceProfile" type="object">
Voice profile object (tone, style, ...).
</ParamField>

<ParamField body name="websiteUrl" type="string">
The business website. Required to use extract.
</ParamField>

### `update_brand_kit`

<Badge>write</Badge>

Update a brand kit: identity fields (business name, positioning, audience, voice profile, visual style, content strategy), its brand media, which kit is the DEFAULT, and which tracked accounts it is LINKED to. Only the fields you pass change. Get the current kit first with get_brand_kit. Requires the brandkit:write scope. THREE MODES, chosen by what you pass: (1) pass brandKitId to patch one kit; (2) pass orderedIds ALONE to reorder the whole set, which is collection-level because ordering is a property of the set and a per-kit position would let two kits claim one slot, so pass every id in the order you want; (3) pass brandKitId + extract:true to RE-RUN website extraction, which returns immediately and fills the kit in the background from its websiteUrl (poll extractionStatus via get_brand_kit). logos/assets/sections/brandAccounts/inspirationAccounts are DECLARATIVE: a patch REPLACES the whole list, so pass the full set and use [] to clear. THIS IS ALSO HOW YOU ADD NEW MEDIA TO A KIT: a logo or asset entry names either a url it already has, or outputId to bring in a generation that is not in the kit yet ('\<id\>', or '\<id\>-2' for variation 2 of a batch), whose bytes get COPIED into the kit so trashing that generation later cannot empty it. To add a logo, read the kit, append one entry, and send the whole list back; sending an outputId twice adds it twice. brandAccounts are the account owner's OWN profiles (performance), inspirationAccounts are competitors and creators they watch; they are separate lists because they mean opposite things. AN ENTRY IS EITHER a tracked-account id you already have, OR \{ platform?, handleOrUrl \} to ADD a profile that is not tracked yet, which is what STARTS ingesting its posts (a full profile url carries its own platform, so platform is only needed for a bare handle). isDefault only accepts true (passing false would leave the account with no default at all, so to move the default, name the kit that should hold it).

<ParamField body name="assets" type="object[]">
The kit's brand assets, each \{ url | outputId, name? \}. REPLACES the list; [] clears it.
</ParamField>

<ParamField body name="audience" type="object">
Audience object (free-form).
</ParamField>

<ParamField body name="brandAccounts" type="value[]">
The account owner's OWN profiles. Each entry is a tracked-account id, or \{ platform?, handleOrUrl \} to ADD one and start ingesting it. REPLACES the list; [] clears it.
</ParamField>

<ParamField body name="brandKitId" type="string">
The brand kit id. Omit ONLY when reordering with orderedIds.
</ParamField>

<ParamField body name="businessName" type="string">
The business or creator name this kit represents.
</ParamField>

<ParamField body name="contentStrategy" type="object">
Content strategy object (free-form).
</ParamField>

<ParamField body name="designPrinciples" type="string[]">
Design rules to hold to, one per entry. REPLACES the list; [] clears it.
</ParamField>

<ParamField body name="extract" type="boolean">
Re-run website extraction for this kit. Returns immediately; poll extractionStatus.
</ParamField>

<ParamField body name="inspirationAccounts" type="value[]">
Competitor/creator profiles they watch. Same entry shape as brandAccounts. REPLACES the list; [] clears it.
</ParamField>

<ParamField body name="isDefault" type="boolean">
Make this the default kit, un-defaulting every other.
</ParamField>

<ParamField body name="logos" type="object[]">
The kit's logos, each \{ url | outputId, name?, is_primary?, layout?: horizontal|stacked|icon|wordmark, colorMode?: full_color|light|dark|grayscale \}. REPLACES the list; [] clears it. Exactly one ends up primary (the kit's cover); name none and the first wins.
</ParamField>

<ParamField body name="name" type="string">
Rename the kit. This is the label in the UI, not the business name.
</ParamField>

<ParamField body name="nicheDefinition" type="string">
The niche this brand operates in. Keeps generated angles on-topic.
</ParamField>

<ParamField body name="orderedIds" type="string[]">
Reorder mode: every brand kit id, in the order you want them. Pass this alone.
</ParamField>

<ParamField body name="positioning" type="object">
Positioning object (free-form).
</ParamField>

<ParamField body name="primaryOffer" type="string">
What this business sells, in one line. Grounds copy in what is actually being promoted.
</ParamField>

<ParamField body name="sections" type="object[]">
The kit's curated sections, each \{ tab, sectionName, sortOrder?, fields? \}. REPLACES the set, keyed by (tab, sectionName); a section left out is ARCHIVED, never deleted. Array position is the default order.
</ParamField>

<ParamField body name="visualStyle" type="string">
The look in words, e.g. 'warm film grain, muted earth tones'. Grounds image and video prompts.
</ParamField>

<ParamField body name="voiceProfile" type="object">
Voice profile object (tone, style, ...).
</ParamField>

<ParamField body name="websiteUrl" type="string">
The business website. Stored as a reference; it does not re-extract on its own.
</ParamField>

### `list_brand_knowledge`

<Badge>read</Badge>

The complete, paginated index of a brand kit's knowledge items (titles and metadata, no bodies). Use it to browse what exists, or to find an item's id before get_brand_knowledge or remove_brand_knowledge. For relevance retrieval, use search_brand_knowledge instead. Requires the brandkit:read scope.

<ParamField body name="brandKitId" type="string" required>
The brand kit id.
</ParamField>

<ParamField body name="limit" type="integer">
How many to return (default 50).
</ParamField>

<ParamField body name="offset" type="integer">
Pagination offset.
</ParamField>

### `get_brand_knowledge`

<Badge>read</Badge>

Get one knowledge item's stored body by id (the capped anchor text; the full document is embedded for search, not stored verbatim). Use search_brand_knowledge for the deep content. Requires the brandkit:read scope.

<ParamField body name="brandKitId" type="string" required>
The brand kit id.
</ParamField>

<ParamField body name="knowledgeId" type="string" required>
The knowledge item id (from list_brand_knowledge or search_brand_knowledge).
</ParamField>

### `add_brand_knowledge`

<Badge>write</Badge>

Add an item to a brand kit's knowledge base so it can be searched later. This is how the brand's knowledge grows over time: capture a lesson learned, a brand decision, an asset description, an article, or a video. Source can be text (a note), url (a page to scrape), youtube (a video transcript), or file. For a file, pass either fileData (base64, best for small documents and images) or fileUrl (a hosted URL the server fetches, needed for large files and video/audio). Requires the brandkit:write scope.

<ParamField body name="brandKitId" type="string" required>
The brand kit id.
</ParamField>

<ParamField body name="sourceType" type="`text` \| `url` \| `youtube` \| `file`" required>
How the content is provided.
</ParamField>

<ParamField body name="fileData" type="string">
For sourceType "file": base64-encoded file bytes (small documents and images).
</ParamField>

<ParamField body name="fileExt" type="string">
For sourceType "file": the extension without a dot, e.g. "pdf". Inferred from fileUrl when omitted.
</ParamField>

<ParamField body name="fileUrl" type="string">
For sourceType "file": a hosted URL the server fetches (large files, video, audio).
</ParamField>

<ParamField body name="text" type="string">
For sourceType "text": the note body.
</ParamField>

<ParamField body name="title" type="string">
Optional title (otherwise derived from the content).
</ParamField>

<ParamField body name="url" type="string">
For sourceType "url" or "youtube": the link.
</ParamField>

### `remove_brand_knowledge`

<Badge>write</Badge>

Remove a knowledge item and its embedding chunks from a brand kit's knowledge base. Requires the brandkit:write scope.

<ParamField body name="brandKitId" type="string" required>
The brand kit id.
</ParamField>

<ParamField body name="knowledgeId" type="string" required>
The knowledge item id to remove.
</ParamField>

### `search_brand_knowledge`

<Badge>read</Badge>

Semantic search over a brand kit's knowledge base (everything the owner has uploaded: notes, docs, articles, video transcripts). Returns the most relevant passages, ranked. This is the deep-grounding read: use it to pull what the brand has said about a topic before drafting or deciding. Requires the brandkit:read scope.

<ParamField body name="brandKitId" type="string" required>
The brand kit id (from list_brand_kits / get_brand_kit).
</ParamField>

<ParamField body name="query" type="string" required>
What to search for, in natural language.
</ParamField>

<ParamField body name="limit" type="integer">
Max matches (default 8).
</ParamField>

<ParamField body name="threshold" type="number">
Minimum similarity 0-1 (default 0.45).
</ParamField>

